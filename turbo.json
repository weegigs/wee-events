{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**"]
    },
    "compile": {
      "dependsOn": ["^compile"],
      "outputs": ["lib/**"]
    },
    "test": {
      "dependsOn": ["^compile"]
    },
    "lint": {
      "cache": true
    },
    "clean": {
      "cache": false
    }
  }
}